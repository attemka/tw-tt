import styled from '@emotion/styled'

export const TooltipWrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  width: fit-content;
  padding: 8px;
  background: white;
  gap: 8px;
  border: 2px solid gray;
  border-radius: 8px;
`

export const TickerNaming = styled.span`
  font-weight: bold;
  margin-right: 2px;
`
